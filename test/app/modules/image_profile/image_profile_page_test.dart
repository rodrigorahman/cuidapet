import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:cuidapet/app/modules/image_profile/image_profile_page.dart';

main() {
  testWidgets('ImageProfilePage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ImageProfilePage(title: 'ImageProfile')));
    //  final titleFinder = find.text('ImageProfile');
    //  expect(titleFinder, findsOneWidget);
  });
}
