import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:cuidapet/app/modules/address/address_confirm/address_confirm_page.dart';

main() {
  testWidgets('AddressConfirmPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AddressConfirmPage(title: 'AddressConfirm')));
    //  final titleFinder = find.text('AddressConfirm');
    //  expect(titleFinder, findsOneWidget);
  });
}
