import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:cuidapet/app/modules/address/address_confirm/address_confirm_controller.dart';
import 'package:cuidapet/app/modules/address/address_module.dart';

void main() {
  initModule(AddressModule());
  // AddressConfirmController addressconfirm;
  //
  setUp(() {
    //     addressconfirm = AddressModule.to.get<AddressConfirmController>();
  });

  group('AddressConfirmController Test', () {
    //   test("First Test", () {
    //     expect(addressconfirm, isInstanceOf<AddressConfirmController>());
    //   });

    //   test("Set Value", () {
    //     expect(addressconfirm.value, equals(0));
    //     addressconfirm.increment();
    //     expect(addressconfirm.value, equals(1));
    //   });
  });
}
