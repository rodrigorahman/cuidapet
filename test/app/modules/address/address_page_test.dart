import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:cuidapet/app/modules/address/address_page.dart';

main() {
  testWidgets('AddressPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AddressPage(title: 'Address')));
    //  final titleFinder = find.text('Address');
    //  expect(titleFinder, findsOneWidget);
  });
}
