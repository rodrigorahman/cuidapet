import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:cuidapet/app/modules/chat_list/chat_list_page.dart';

main() {
  testWidgets('ChatListPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ChatListPage(title: 'ChatList')));
    //  final titleFinder = find.text('ChatList');
    //  expect(titleFinder, findsOneWidget);
  });
}
