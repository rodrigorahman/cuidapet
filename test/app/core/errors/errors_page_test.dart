import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:cuidapet/app/core/errors/errors_page.dart';

main() {
  testWidgets('ErrorsPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ErrorsPage(title: 'Errors')));
    //  final titleFinder = find.text('Errors');
    //  expect(titleFinder, findsOneWidget);
  });
}
