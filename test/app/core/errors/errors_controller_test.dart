import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:cuidapet/app/app_module.dart';

void main() {
  initModule(AppModule());
  // ErrorsController errors;
  //
  setUp(() {
    //     errors = AppModule.to.get<ErrorsController>();
  });

  group('ErrorsController Test', () {
    //   test("First Test", () {
    //     expect(errors, isInstanceOf<ErrorsController>());
    //   });

    //   test("Set Value", () {
    //     expect(errors.value, equals(0));
    //     errors.increment();
    //     expect(errors.value, equals(1));
    //   });
  });
}
